# 🎙 Приложение Speech Recognizer Fast

Проект выполняет **распознавание речи из аудиофайлов** с помощью библиотеки **faster-whisper**, обеспечивая высокую скорость и низкое потребление ресурсов.  
Поддерживаются популярные форматы: `.ogg`, `.mp3`, `.wav`, `.flac`.

Перед распознаванием аудио автоматически **конвертируется в WAV** (моно, 16 кГц), **нормализуется** и сохраняется в каталог  
`audio_files/normalized`. Затем загружается модель **faster-whisper-large-v2**, выполняется транскрибация и возвращается готовый текст.

Проект поддерживает работу как на **CPU**, так и на **GPU (CUDA)**.  
Для ускорения можно использовать модели с **Hugging Face**, а для экономии памяти — автоматическую очистку после обработки.

Опционально доступна **суммаризация текста** через библиотеку `transformers`, если она установлена.

---

### 🐳 Пример сборки Docker-образа

Перед запуском необходимо создать файл .env, переименовав шаблон example.env.txt и указав в нём необходимые токены для работы Telegram-бота:

```
NAME_BOT=[Имя бота]
API_KEY=[Ключ к боту телеграмма]
HF_TOKEN=[Токен от Hugging Face]
ADMIN_ID=[ID администратора]
```

После этого можно собрать Docker-образ командой:

```bash
docker build -f base.Dockerfile -t my-python-base:latest .
```

🔗 Модели

openai/whisper-large-v2

Systran/faster-whisper-large-v2


#### История изменений

##### Версия v1.1.0 — 08.11.2025
- Добавлена обработка **видео и кружочков (video_note)**  
- Улучшена устойчивость к ошибкам при загрузке и извлечении аудио  
- Оптимизировано логирование и очистка временных файлов  
- Добавлена версионность

##### 🚀 Версия v1.0.0
- Добавлена предварительная обработка аудио (preprocess_audio): нормализация, конвертация, добавление тишины
- Оптимизирована очередь задач и обработка коротких файлов
- Улучшено логирование и обработка ошибок
- Добавлены комментарии и форматирование кода
- Поддержка видеофайлов (.mp4, .mov, .mkv) с извлечением звука
- Добавлена очистка временных файлов после обработки
- Улучшено деление текста на блоки по 4000 символов
- Первая версия: базовая транскрибация через faster-whisper
- Поддержка Telegram-бота
- Простейшая очередь задач и обработка аудиофайлов
